target: diagnostics/appearance_rerank/seqmap_active.txt
evaluate_cmd: python autoresearch/eval_appearance_rerank.py
metric: pooled_hota
metric_direction: higher
metric_grep: ^pooled_hota:
time_budget_minutes: 15
created: 2026-06-14
note: phase-3 appearance re-rank loop (off exp/pathB-lidar-17d). Optimizes which appearance exprs get CLIP-L/14 spatial rerank (subset expansion). Uses pre-built ship GMC caches -> ~1-2 min/iter. Baseline 45.089 single-seed.
